# 2026-07-05 Dev 1.0.0 Release Reset

## Scope

- Reset Electron package metadata from `1.1.1` to `1.0.0`.
- Rewrote `README.md` as a concise public dev `1.0.0` landing document instead of a long historical release log.
- Updated `OVERVIEW.md` operational notes to route future release work through dev `1.0.0`.
- Planned GitHub release cleanup: delete previous `v1.1.1`, `v1.1.0`, `v1.0.2`, and `v1.0.1` release entries only after validation, push, and artifact creation succeed, then publish `dev-1.0.0`.

## Validation

- Run `cd DESKTOP; npm.cmd test`.
- Stop matching packaged Newmark release processes before packaging.
- Run `cd DESKTOP; npm.cmd run dist:portable`.
- Compute SHA256 hashes for the `1.0.0` portable executable and unpacked zip pack.
- Push the validated commit to `origin/master`.
- Recreate the GitHub release as prerelease `dev-1.0.0`.

Completed local validation:

- `cd DESKTOP; npm.cmd test`
  - Passed with `862 passed`, `0 failed`.
- `cd DESKTOP; npm.cmd run dist:portable`
  - Passed and verified the portable executable, `win-unpacked`, `app.asar`, embedded Lucide UI assets, `config.example.json`, icon patching, zip pack, and packaged release CLI smoke.
- `cd DESKTOP; npm.cmd run release:111-cli-smoke`
  - Passed file/GitHub audit, Git branch current, GitHub fork status with structured fallback support, remote repository security audit, Computer Use dry-run move/scroll/target guard, OpenSSH tool availability, and CLI Agent terminal takeover.
  - VM SSH real link was skipped because `NEWMARK_RELEASE_SSH_HOST` and `NEWMARK_RELEASE_SSH_USER` were not set.
- Secret diff scan completed without matches.

Artifact SHA256:

- `release/Newmark-Agent-1.0.0-portable-x64.exe`
  - `8EC75C700536E9F3A51DCBA96B495522583DBF1BCB32BE35941FA0C0232EF0DC`
- `release/Newmark-Agent-1.0.0-win-unpacked-x64.zip`
  - `375174854CCBA0018CD974666E63CD6FCFDC942AC3EECDFA49EA51031F343591`

Computer Use desktop screenshots must not be saved during this release reset.
