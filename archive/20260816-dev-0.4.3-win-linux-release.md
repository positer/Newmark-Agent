# dev-0.4.3 打包 + UAC 安装 + win/linux release（2026-08-16）

## 背景 / 目标

用户要求：打包、本机 UAC 安装、发布 win + linux。dev-0.4.3 源码（含 thinking_tier_map 映射、缓存压测、task-list/Guide 等切片）已 commit（`dd08013`）并 push，需要移动 tag、构建双平台产物、UAC 安装并发布 GitHub prerelease。

## 发现与决策

- 本地 tag `dev-0.4.3` 原先指向 `01ae11d`（上一 dev-0.4.3 切片），远端没有 dev-0.4.3 tag；本次发布应包含全部 dev-0.4.3 内容 → 删除旧 tag 后新建指向 `dd08013`。
- `npm run dist:windows-release` = `test:full-release`（完整门禁）+ `build:clean` + `dist-portable.cjs`（electron-builder MSI/ZIP），本地一次通过。
- UAC 安装沿用 `msiexec /i <msi> /qb /norestart` 提权方式；路径含空格需引号包裹（首次 1639 无效命令行，修正后 exit 0）。
- Linux 构建经 WSL Ubuntu-24.04 隔离文件系统：`npm ci` → 完整 `npm test` → `benchmark:linux-agent-latency` → `electron-builder --linux` → rsync 回 `release/`。
- 首次 `gh release create`（一次性带 5 资产）在 Windows ZIP 上传阶段超时，留下 draft + untagged 状态的 release（3 个 Linux 资产已传）；删除后以 `--verify-tag` 重建（URL 正确指向 `releases/tag/dev-0.4.3`），再逐资产 `gh release upload` 完成 5 资产，`--prerelease` 标记。
- `release/RELEASE_NOTES-dev-0.4.3.md` 被打包流程清理，重新生成后再引用。

## 执行的操作（可复现）

1. `git tag -d dev-0.4.3 && git tag dev-0.4.3`（指向 `dd08013`）；`git push origin master`（`369d68c..dd08013`）；`git push origin dev-0.4.3`。
2. Windows 打包（后台）：`cd DESKTOP; npm run dist:windows-release` → EXIT=0（test:full-release 全绿，含 SSH TUI stress、release CLI smoke、context-compress CLI stress 9 请求、console-wrapper boundary version=0.4.3）。
3. UAC 安装：`Start-Process msiexec -Verb RunAs -Wait -PassThru -ArgumentList "/i <msi> /qb /norestart"` → exit 0。
4. 安装验证：`Newmark Agent.exe` FileVersion/ProductVersion = 0.4.3.0；安装目录与打包 `app.asar` SHA-256 一致；`Newmark.exe --version` → 0.4.3 exit 0。
5. Linux 构建（后台）：`cd DESKTOP; npm run dist:linux`（WSL Ubuntu-24.04）→ EXIT=0（Linux 完整 `npm test` 全绿 + electron-builder AppImage/deb/unpacked ZIP + rsync 回 `release/`）。
6. Release：写 `release/RELEASE_NOTES-dev-0.4.3.md`；`gh release create dev-0.4.3 --title dev-0.4.3 --notes-file ... --prerelease --verify-tag`；`gh release upload` 逐个上传 5 资产（--clobber）。

## Release 资产哈希（release/）

| 文件 | 大小 | SHA-256 |
| --- | --- | --- |
| Newmark-Agent-0.4.3-x64.msi | 226,093,135 | `21A5F9F98D12306CC9E4AC823A88E9949F32137532940BA50FB558D822A8098A` |
| Newmark-Agent-0.4.3-win-unpacked-x64.zip | 291,951,403 | `0BC22DA0A9C9228A0C29CCA9A56A38480215C3A411BCEFB7771B080C5CA0446B` |
| Newmark-Agent-0.4.3-x86_64.AppImage | 176,073,699 | `12712168258376DD4BA1FCB1D3A0C3CA884B6376D2CF27C70157A2C3D9124675` |
| Newmark-Agent-0.4.3-amd64.deb | 135,669,308 | `07D43FF902A35F38C8D1C062EC441D9CF06518D0A067EE3623A9F613A2641C4D` |
| Newmark-Agent-0.4.3-linux-unpacked-x64.zip | 172,186,536 | `C52523D143313D93D6FE8AA0B0E26045133F94B6352F9CA740F45E3138DD9FA9` |

## 结果 / 验证

- 远端 master = `dd08013`，tag `dev-0.4.3` 已推送；工作区仅剩 TUI/test 临时未跟踪文件（非本次改动）。
- Program Files 安装 0.4.3.0，app.asar 与打包目录逐字节一致（`8B241F04D761003ACE3C5C61F72C32F2A37A55406B7BB749CB3E5769C6AB4E1A`）。
- GitHub Release：https://github.com/positer/Newmark-Agent/releases/tag/dev-0.4.3（prerelease=true，draft=false，5 资产齐全，尺寸与本地一致）。

## 状态

- 已 commit（`dd08013` 已 push）、已打 tag `dev-0.4.3`、已发布 GitHub prerelease（Windows MSI/ZIP + Linux AppImage/deb/unpacked ZIP）。
- macOS 与 HarmonyOS 产物不在本次切片。

## 备注

- 大资产（>200MB）建议 `gh release create` 只建元数据，再逐资产 `gh release upload`，避免单命令超时留下 draft/untagged 残留。
- WSL Linux 构建临时根由脚本自动清理；`release/` 保留全部 5 产物 + release notes。
