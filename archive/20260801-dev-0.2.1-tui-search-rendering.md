# dev-0.2.1 TUI search, model identity, and rendering

Date: 2026-08-01

## Scope

- Study current Claude Code, Codex CLI, and OpenCode terminal interaction/performance patterns.
- Remove non-functional TUI placeholders, especially Overview/Detail/Search/Reindex labels in Memory Lab and the synthetic root dashboard.
- Add Memory Lab tag search.
- Show the current model next to the active Conversation title.
- Reduce terminal rendering overhead.
- Advance source metadata to dev-0.2.1.

## Source review

- Claude Code documents event-driven status updates with a 300 ms debounce and exposes current model identity as persistent session state: https://code.claude.com/docs/en/statusline
- Codex CLI exposes `model-with-reasoning` as a default status-line item and bounds terminal resize reflow through TUI configuration: https://github.com/openai/codex/blob/main/codex-rs/core/config.schema.json
- OpenCode owns the terminal viewport through SolidJS/OpenTUI and keeps session identity in the terminal title: https://github.com/anomalyco/opencode/blob/dev/packages/opencode/src/cli/cmd/tui/app.tsx

## Implementation

- Removed the fabricated Overview route/dashboard and standalone ROOT row; startup now enters Conversations.
- Removed inactive Memory Lab Overview, Detail, Search tags, and Reindex labels. `/` now enters a real case-insensitive tag filter over the loaded snapshot; Enter follows and Esc clears.
- Removed the generic mock Details modal and action copy that claimed unavailable inspectors/searches.
- Added `activeModelName` to real/mock TUI snapshots. Conversation titles show a fixed deployment display name or `Auto · resolved-model`.
- Added an event-loop paint scheduler that coalesces bursts and skips identical frames. Spinner cadence is 250 ms.
- Updated DESKTOP and TUI package metadata to `0.2.1`.

## Verification

- Node syntax checks passed for all modified TUI sources and tests.
- Focused TUI regression: 51/51 passed.
- The suite includes isolated real-Core registration/persistence, Memory Lab tag filtering, fixed/Auto model title rendering, burst coalescing, identical-frame suppression, CJK widths, narrow/wide terminal matrices, and long-menu cursor visibility.

- Final `cd DESKTOP && npm.cmd test` exited 0 in 344.4 seconds.
- The final unified run passed the DESKTOP feature/specialized gates, TUI 51/51, launcher 17/17, real loopback SSH PTY including four restarts, Ubuntu 24.04 WSL PTY, CLI contract/Anthropic/Computer Use gates, and 18-conversation GUI/TUI/CLI shared-backend stress with 21 provider requests.
- The only emitted warnings were the pre-existing esbuild/jsdom `xhr-sync-worker.js` externalization warnings; no gate failed.
