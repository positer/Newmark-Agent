# dev-0.1.9 Automation and Bash Temporary-Root Stress Test

Date: 2026-07-27

## Scope

Run automation scheduling, conversation behavior, Build ownership, native Bash, host fallback, cancellation, and real PTY behavior against an isolated local temporary directory. No test state is written into the normal `~/.Newmark` runtime.

## Accepted run

- Command: `cd DESKTOP && npm.cmd run test:automation-bash-stress`
- Result: pass
- Duration: 13.851 seconds after build
- Evidence root during acceptance: `C:\Users\12252\AppData\Local\Temp\newmark-automation-bash-stress-RQKDrD`
- Machine-readable report during acceptance: `stress-report.json` in the evidence root
- Cleanup: the accepted root, prior failed roots, the automation test root, and the temporary upstream clone were deleted after audit under the user's explicit authorization

## Automation results

- 34 configured schedules.
- 56 total triggers and 56 unique Build run IDs.
- 56 completed work runs verified again from the two workspace `conversations/state.json` files.
- 32 `new`-mode triggers produced 32 unique conversations.
- 24 `existing`-mode triggers retained their configured conversation IDs.
- Eight due schedules executed from the real `AutomationManager.start()` wall-clock interval.
- Forty overlapping `tick()` calls attempted to re-enter one blocked automation; the runner started exactly once and persisted one completed run.
- Every receipt resolved to the configured workspace path.

## Bash and terminal results

- 16 concurrent persistent native Bash sessions.
- 320 sequential commands inside those sessions with retained per-session `cwd` and isolated log files.
- 64 parallel native one-shot pipeline/file round trips.
- 12 cooperative cancellation runs; none reached the post-sleep marker.
- `git --version` correctly selected the host-shell fallback and exited successfully.
- Four simultaneous PowerShell sessions completed marker round trips through the production `node-pty`/ConPTY boundary.

## Initial harness repair

The first attempt stopped before stress execution because the test did not create the temporary ConfigManager root before saving `config.json`. The harness was corrected to create runtime roots explicitly, then three complete stress executions passed; the final accepted run additionally verifies durable Build records. All failed and accepted temporary roots were deleted after the release audit.

## Reproduction

```powershell
Set-Location 'C:\Users\12252\Desktop\Files\Code\Newmark Agent\DESKTOP'
npm.cmd run test:automation-bash-stress
```
