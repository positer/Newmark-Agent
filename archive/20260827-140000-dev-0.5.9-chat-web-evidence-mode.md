# dev-0.5.9 Chat 联网取证模式与移动队列头精简

时间：2026-08-27 14:00 Asia/Shanghai

## 用户要求

- 移动端去掉队列“排队对话”标题，仅保留 `n 条待处理`。
- PC 和移动端新增与 Build/Plan 等位的 Chat。
- 移动端本地对话开放 Chat。
- Chat 只允许 `web_search`、`web_fetch`，拒绝写入权限。
- Agent 联网搜索取证后尽快总结回答。

## 实施边界

Chat 使用双层强制策略：

1. 提供给模型的工具 schema 只包含 `web_search`、`web_fetch`。
2. PC `evaluateToolPolicy`、移动端 ViewModel 与 `LocalToolExecutor` 在执行边界再次检查，旧上下文或伪造调用不能触达文件、终端、应用、设置、Memory Lab、任务、浏览器控制或系统能力。

Chat 系统提示要求先搜索，搜索摘要不足时抓取权威/一手来源，证据充分后立即收束为简洁回答并给出链接。

## 验证

- PC：`npm run typecheck`、完整 build、`chatModePolicyVerify`、`verify.js`；1673 assertions passed。
- Android：全量 JVM 测试、Vital Lint、R8、Release assembly；全部成功。
- Android Release 使用 debug signing config，仅供本地预发布安装验证。
