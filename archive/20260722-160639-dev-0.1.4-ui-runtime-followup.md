# dev-0.1.4 UI and Runtime Follow-up

## Scope

- Make title-bar minimize enter the taskbar instead of hiding to tray.
- Render light-theme error notices with the active Newmark palette.
- Replace the square native model popup with a rounded accessible Newmark listbox.
- Let each chronological edited-file row expand its own diff instead of appending a duplicate edited-files section.
- Keep Subagents alive beyond the former hard-coded two-minute cutoff while retaining cooperative parent cancellation.
- Render Subagent calls as clickable status chips inside the Build block and open the exact peer work history from the chip.
- Name exact Skill and MCP activity inside the owning Build block.
- Repair Memory Lab overview deselection, pointer release, high-frequency drag performance, unlimited overview zoom, far-zoom dot rendering with hover/focus name tooltips, and desktop IPC preservation of hierarchical `tagPaths`.

## Implementation boundary

- `DESKTOP/src/main.ts` owns the taskbar/tray lifecycle split.
- `DESKTOP/src/core/agent.ts` removes the independent peer timeout and retains the existing active-peer cancellation graph.
- `DESKTOP/src/ui/index.html` owns semantic notice tokens, the custom model listbox, inline file diffs, Skill/MCP labels, and the bounded/on-demand Memory Lab renderer.
- `DESKTOP/src/tests/verify.ts` locks the source and UI contracts against regression.

## Verification

- `npm.cmd run typecheck`: passed.
- `npm.cmd run build`: passed; only the existing jsdom `xhr-sync-worker.js` externalization advisory remains.
- Main feature verifier: `1238/1238` assertions passed.
- `node dist/tests/visualPreferencesVerify.js`: passed after replacing the initial fixed blur with the project glass token.
- Complete `npm.cmd test`: passed, including runtime isolation, Subagent, visual preferences, tool execution, Auto routing, model validation, and CLI contracts.

## Packaged verification

- Windows release build passed its bundled CLI smoke, MSI smoke (`22` assertions), unpacked ZIP smoke, model settings UI smoke, tray/direct-close lifecycle smoke, and Memory Lab UI smoke. The Memory Lab gate exercises hierarchy persistence, far-zoom dot tooltips, selection toggling, root/child navigation, search, and component preview against the packaged renderer.
- Linux release build passed the complete test suite inside Ubuntu 24.04 plus the Agent latency gate (`hotFirstEventP95Ms=20.920807`, `hotFirstTokenP95Ms=21`). Native GUI, explicit/tray-shared exit lifecycle, AppImage, unpacked ZIP, and DEB smokes all passed. The exit gate confirmed same-root relaunch and no retained process or single-instance lock.

## Release assets

| Asset | Bytes | SHA-256 |
| --- | ---: | --- |
| `Newmark-Agent-0.1.4-x64.msi` | 128905911 | `E72BCA2EA7C08C7FC29D09C5222645684D2F33A5CEEF8BD0F4FFB013AD227974` |
| `Newmark-Agent-0.1.4-win-unpacked-x64.zip` | 167238699 | `2A3CCB854463466A98242778B35944A43C162056DAB18D998AD9211064A23704` |
| `Newmark-Agent-0.1.4-x86_64.AppImage` | 148093095 | `18F845D00F3A848441F0E403FA253CBA97928AF57382578188B40A224C326A5A` |
| `Newmark-Agent-0.1.4-amd64.deb` | 115144912 | `F4F4E5EBF78BD235D7AAAFF02946F64C50662436438F649C3E4200D169AE429C` |
| `Newmark-Agent-0.1.4-linux-unpacked-x64.zip` | 144391341 | `961ED961EBF5AF54AD5406677249CF6CCDB6114A0AC0C187D021CE8BC92EC6AD` |

Git tag and GitHub Release asset verification follow after publication.
